import React from "react";

import CustomButton from "@/components/CustomButton";

const Payment = () => {

    return (
        <>
            <CustomButton
                title="Confirm Ride"
                className="my-10"
                onPress={() => {}}
            />
        </>
    );
};

export default Payment;
